# W-03 handoff: wire typed standalone action handlers

## Assignment

- Task ID and title: W-03, Wire typed standalone action handlers
- Owner/agent and independent reviewer: `/root/x05_implementer`; `/root/x05_reviewer`
- Base commit: `13e32c7b8f96a9d322ab933214d4a54e687d7a98`
- Branch/worktree or shared-checkout ownership, repository-relative public notation: shared `main` checkout; this lane owned only `internal/actions/` and this handoff
- Owned files and generated outputs: `internal/actions/actions.go`, `actions_test.go`, `arr.go`, `client.go`, `descriptor.go`, `filesystem.go`, `refresh.go`; no generated output
- Dependencies verified at commits: coordinator dispatch checkpoint `13e32c7`; W-01, W-02, F-04, F-05, X-06, X-07, X-08 and X-09 were recorded as dependencies before dispatch
- Required acceptance IDs and exact planned commands: A-13, A-14, A-17, A-18, A-20, A-24, A-33 and A-60; focused tests, race, vet, root and nested module checks, module verification, Linux amd64/arm64 CGO-free builds, generation/API/Vacuum, lint/architecture, planning and guardrails

## Contract and work

- Linked specification sections and relevant invariants: `docs/specs/spec-001-media-reconciliation/http-api.md` action constraints and recovery; `docs/specs/spec-001-media-reconciliation/data-and-recovery.md` observe-before-write and uncertainty; `docs/plans/implementation.md` W-03; A-13/A-14/A-17/A-18/A-20/A-24/A-33/A-60 in `docs/verification/acceptance.md`
- Intended result and capability limits: one typed `execution.Handler` per v0.0.1 action kind. Handlers use only Mastarr ports, perform bounded read-before-write checks, preserve exact effect identities, honor cancellation/deadlines and return typed capability/state failures. No SQL, HTTP, generated upstream DTO, arbitrary command or workflow composition enters this package.
- Changes completed / remaining:
  - Added independent Arr registration and import handlers. Registration observes a complete catalog and preserves unspecified fields. Import binds an exact native preview revision and selected file set, validates rejection evidence, then reports per-file association read-back.
  - Added copy, hardlink, move, rename, trash, restore and delete handlers over exact root-relative manifests. Source identity, size, digest, destination identity, child expansion, bounds and linked-client stopped prerequisites are checked before dispatch. `native_client` move/rename calls the normalized download-control port; it cannot fall back to a filesystem move.
  - Added independent client stop and metadata-only remove handlers, descriptor delete with explicit acknowledgement, and Jellyfin refresh with item-scope blocking and refresh-acceptance evidence kept separate from later availability.
  - Added strict bounded intent decoding with recursive duplicate-key rejection, capability validation, exact connection/reference checks and synthetic tests for partial import, preview mismatch, linked-client safety, native executor routing, invalid effects, custom retention and idempotency.
  - Native Arr registration/import remain blocked when the root adapter reports G-01 unknown or unsupported. A planning `ImportPredicate` has no preview revision field, so conversion to `arr.import` rejects that form until a later workflow/API envelope supplies the exact preview binding. No live upstream write was enabled.
- Shared contract changes requested from coordinator: none
- Changed paths and tested commit: product `fc2bab90bd3db21c6801842babee6b473e2742da`

## Verification

| Command or scenario | Commit / fixture version | Result / exit status | Evidence path |
| --- | --- | --- | --- |
| `GOWORK=off go test ./internal/actions -count=1 -timeout=120s` | `fc2bab90bd3db21c6801842babee6b473e2742da`; synthetic ports | Passed, exit 0 | `internal/actions/actions_test.go` |
| `GOWORK=off go test -race ./internal/actions -count=1 -timeout=120s` | same | Passed, exit 0 | `internal/actions/actions_test.go` |
| `GOWORK=off go vet ./internal/actions` | same | Passed, exit 0 | `internal/actions/` |
| `GOWORK=off go test ./internal/... -count=1 -timeout=180s` | same; root synthetic fixtures | Passed, exit 0 | root `internal/` packages |
| `./scripts/generate.sh --check` | same; committed generated tree | Passed, exit 0 | `scripts/generate.sh` |
| `python3 scripts/check-architecture.py` | same | Passed, exit 0 | `scripts/check-architecture.py` |
| `./scripts/check-lint.sh` | same; root, UI, tools and six client modules | Passed, exit 0 | `scripts/check-lint.sh` |
| `python3 scripts/check_planning.py` | same | Passed, exit 0; 53 tasks and 60 acceptance cases | `docs/plans/`, `docs/execution/tasks.json` |
| `./scripts/check-guardrails.sh --fast` | same; staged-contract and generation checks | Passed, exit 0 | `scripts/check-guardrails.sh` |
| `GOWORK=off GOPROXY=off GOSUMDB=off ./scripts/check-guardrails.sh --ci` | same; all root/UI/tools/client modules | Passed, exit 0; tests, race, vet, mod verification and Linux amd64/arm64 CGO-free builds | `scripts/check-guardrails.sh` |
| `TestRegistrationObserveBeforeG01BlockedDispatch` | same; synthetic Arr capability unknown | Passed; catalog read occurred and registration writes stayed at zero | `internal/actions/actions_test.go` |
| `TestImportRequiresExactPreviewBeforeAnyWrite` | same; synthetic preview revision mismatch | Passed; preview read occurred and import command calls stayed at zero | `internal/actions/actions_test.go` |
| `TestImportReportsPerFilePartialState` | same; one video association and one missing subtitle | Passed; one `already_satisfied` effect and one pending effect, no import dispatch | `internal/actions/actions_test.go` |
| `TestCopyMissingSourceAndDestinationBlocksWithoutWrite` | same; synthetic missing source/destination | Passed; unknown state and zero copy calls | `internal/actions/actions_test.go` |
| `TestNativeMoveUsesLinkedClientAndNeverFilesystemMove` / `TestNativeRenameSelectsExactFileOperation` | same; stopped synthetic client | Passed; normalized native operation called, filesystem move stayed at zero | `internal/actions/actions_test.go` |
| `TestNativeClientRejectsMultiMapBeforeAnyNativeOrFilesystemCall` | same; two-map native intent | Passed; invalid intent before any native/filesystem call | `internal/actions/actions_test.go` |
| `TestDescriptorDeleteRequiresAcknowledgementAndIsIdempotent` | same; synthetic descriptor service | Passed; repeat read-back returned already-satisfied without a second delete | `internal/actions/actions_test.go` |

All test fixtures use in-memory values or synthetic ports. No credentials,
cookies, private coordinates or live media were used. The full guardrail run
also exercised the existing standalone client modules and their committed
generated output; this lane added no generated code.

## Review and integration

- Reviewer identity/role and reviewed commit: pending `/root/x05_reviewer` review of `fc2bab90bd3db21c6801842babee6b473e2742da`
- Findings with severity, reproduction and contract reference: none recorded yet
- Fix commit and regression evidence: none; independent review pending
- Final reviewer decision: pending
- Integrated commit, recorded by coordinator: pending; coordinator owns `docs/execution/state.json`

## Resume checkpoint

- Current state and outstanding uncertainty: product is committed and all listed local checks passed. End-to-end workflow/API wiring remains W-04/C-04 scope. G-01 keeps native Arr writes fail-closed; upstream write capability and live behavior are not claimed.
- Active process or agent ownership, sanitized: no active process; reviewer owns next review step
- Next safe action: independent review, then coordinator records product and handoff SHAs and decides integration
- Blocker and exact input/evidence needed: enabling native Arr registration/import requires approved G-01 evidence and an exact preview-bound workflow envelope; native client move/rename requires the linked control adapter's per-operation capability evidence
- No conflicting writes or unknown files removed: true; only `internal/actions/` and this handoff were changed by this lane
